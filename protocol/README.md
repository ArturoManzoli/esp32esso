# protocol

Single source of truth for contracts shared by firmware and companion apps.

| Document | Scope |
| -------- | ----- |
| [`ble/tier1.md`](ble/tier1.md) | Tier 1 BLE GATT: live state + setpoint |

When a contract changes, update the matching firmware and app code in the
same pull request.
